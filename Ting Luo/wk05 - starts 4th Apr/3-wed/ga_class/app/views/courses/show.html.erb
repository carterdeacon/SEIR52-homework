<h1>SEI Course: <%= @course.name%></h1>

<ul>
<li>Start Date: <%= @course.start.strftime "%A, %B, %e, %Y" %></li>
<li>End Date: <%= @course.end.strftime"%A, %B, %e, %Y" %></li>
<li>Period: <%= @course.period %></li>
</ul>

Student List: 
<% @course.students.each do |student|%>
    <%= link_to student.name, student%>
<% end %>


<div class="controls">
    <%= link_to "Edit Course", edit_course_path%>
    <%= link_to "Delete Course", @course, :method => "delete", :data => {:confirm => " Please confirm "}%>
</div>