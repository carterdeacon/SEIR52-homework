<div class="index-container">
    <h1>All Mountains</h1>

    <% @mountains.each do |mountain| %>

        <p>
            <strong><%= link_to mountain.name, mountain_path(mountain.id) %><strong>
            is located in <%= mountain.country %>.
        </p>

    <% end %>
</div>
