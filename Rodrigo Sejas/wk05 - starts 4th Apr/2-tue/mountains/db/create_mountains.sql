CREATE TABLE mountains (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    length FLOAT,
    avg_gradient FLOAT,
    category TEXT,
    elevation_gain INTEGER,
    image TEXT,
    map TEXT
);