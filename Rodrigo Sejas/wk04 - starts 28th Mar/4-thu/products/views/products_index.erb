<h1>List of Products</h1>

<% @products.each do |product| %>
    <p>
        <a href="/products/<%= product["id"] %>">
            <%= product["brand"]%> <%= product["name"] %>
        </a>
    </p>
<% end %>